/*
 * File:  LNAddAttributesViewController.h
 *
 * About ME『Public：Codeidea / https://githubidea.github.io』.
 * Copyright © All members (Star|Fork) have the right to read and write『https://github.com/CoderLN/Runtime-RunLoop』.
 *
 * 🏃🏻‍♂️ ◕该模块将系统化学习，后续替换、补充文章内容 ~
 *
 *【Runtime 动态添加属性 -> objc.name】
 */

#import "LNBaseViewController.h"

@interface LNAddAttributesViewController : LNBaseViewController

/**
 4、Runtime(动态添加属性)
    场景：给系统的类添加属性的时候,可以使用runtime动态添加属性方法
    本质：动态添加属性,就是让某个属性与对象产生关联
    需求：让一个NSObject类 保存一个字符串 (runtime一般都是针对系统的类)
 */

@end
